%h1 Создание новой метки

= form_for [:admin, @tag], :html => { :id => 'tags' } do |f|
  = render_errors_for @tag
  = f.text_field :name, :placeholder => 'Название'
  = f.text_field :value, :placeholder => 'Стоимость'
  = f.text_field :description, :placeholder => 'Описание'
  .field
    = f.check_box :operational
    = f.label :operational, 'Показывать в списке меток'
  = submit_tag 'Сохранить метку'
  = link_to 'К списку всех меток', admin_dish_tags_path

