%h1 Login

= form_for @user_session, :url => login_path do |f|
  = render_errors_for @user_session
  .field
    = f.label :email
    = f.text_field :email
  .field
    = f.label :password
    = f.password_field :password
  .field
    = f.submit "Login"

