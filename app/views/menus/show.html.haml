%h1= "Menu for <span>#{@menu}</span>".html_safe
%ul.menu
  - @menu.dishes.each do |dish|
    %li= "#{dish.name} - <span class=\"price\">#{number_with_delimiter dish.price}</span>".html_safe
%p= link_to 'Edit menu', edit_menu_path(@menu)

